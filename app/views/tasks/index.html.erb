
<head>
  <link rel="stylesheet" href="index.scss">
</head>
<body>
<% @tasks.each do |task| %>
  <%= link_to task_path(task) do %>   
    <div href="/tasks/#{task.id}"> 
      <div> <%= task.priority %> <%= task.name %> </div>
    </div> 
  </body>
  <% end %>
<% end %>